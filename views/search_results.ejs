<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Note</title>
    <style>
      body {
        background-color: #121212; /* Deep dark background */
        color: #e0e0e0; /* Light grey text for contrast */
        font-family: "Arial", sans-serif;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        margin: 0;
        padding: 20px;
      }
      .note-card {
        position: relative;
        background-color: #1e1e1e; /* Slightly lighter dark shade for the card */
        border-radius: 15px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.5); /* Soft shadow for depth */
        padding: 20px;
        margin: 20px 0;
        width: 80%;
        height: 80vh;
        /* Maximum width for large screens */
      }
      h2 {
        color: #4caf50; /* Bright green for ratings */
        margin-bottom: 15px;
      }
      p {
        font-size: 16px;
        line-height: 1.6;
        margin-bottom: 10px;
      }
      .rating {
        position: absolute;
        bottom: 20px;
        font-weight: bold;
        color: #ffd700;
      }
      @media (max-width: 768px) {
        .note-card {
          width: 90%; /* More width for smaller screens */
        }
      }
    </style>
  </head>
  <body>
    <% if(note.length == 0){ %>
    <h3>No results found!</h3>
    <% }else{ %>
    <div class="note-card">
      <h2><%= note.title %></h2>
      <p><%= note.content %></p>
      <p class="rating"><%= note.rating %>/10</p>
    </div>
    <% } %>
    <!-- Repeat .note-card for additional notes -->
  </body>
</html>
